import 'package:flutter/material.dart';

class Templates extends StatelessWidget {
  const Templates({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ここにテンプレートの画面が出るよ'),
    );
  }
}
